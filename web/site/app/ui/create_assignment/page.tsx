// The form implementation is shared with the legacy URL, but this route lives
// outside the admin-only server_config layout so lecturers can reach it.
export { default } from "../server_config/create_assignment/page";
